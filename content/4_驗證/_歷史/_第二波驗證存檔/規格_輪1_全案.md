# 輪1 實驗規格（主幹定稿 2026-07-05 23:40）

共同約定（所有 worker 適用）：
- 引擎唯讀基底：`round1/engine_base/`（勿改動）。要改引擎的實驗自己 `cp -R` 一份到 `round1/work/<實驗名>/`。
- vault 一律唯讀；所有產出寫 `round1/reports/報告_<實驗名>.md`（結論＋逐格數字表＋方法自述＋縮減聲明）與同名 `.json`（粗數據）。
- 執行：`"/Users/darrenlee/Library/Mobile Documents/iCloud~md~obsidian/Documents/Darren's note/9_9_系統/scripts/vaultpy" <script>`；vaultpy 對 scratchpad 路徑不通就退 `python3`（引擎純標準庫）。
- **單進程、禁 multiprocessing**、禁再開子 agent。seed 基準：20260705＋實驗號×1000＋格號（報告寫明）。
- 檔位不另指定時一律 v1.1 全域檔：閃4 盾10 場5 HP100 自攻10×1 迴復回30限2 安全網起點15遞增 補償首回合1行動；方向一覆寫 HP47/網基3步幅+3（見 `engine_base/setups.py` 現成配置，不要自己手鍵數值）。
- 開工先跑 `tests.py` 確認 58 綠再動工；改引擎的實驗收工前必須全部舊測試照綠＋新測試綠。
- 報告必含「可信度聲明」：樣本、CI、已知偏差、哪些結論只是方向性。

---

## B6 尺 v2（metrics 擴充）——工作目錄 `work/b6_engine_v2/`

把 `engine_base` 複製為 `work/b6_engine_v2/`，只改 metrics.py＋新增 metrics_v2_tests.py。三個新指標，全部從既有 per-turn 紀錄算（如引擎沒逐回合留 HP 軌跡，加最小掛點、不動結算邏輯）：

1. **優勢軌跡**（其餘兩指標的地基）：adv(t) ＝（甲方 HP 總和 − 乙方 HP 總和）÷ 雙方初始 HP 總和之半（方向一用三名合計）。逐局輸出 adv 序列。
2. **翻盤率**：勝者在 t≥2 的任何回合曾 adv ≤ −0.20（以勝者視角）的對局比例。平局不計入分母。
3. **張力三量**：(a) 波動度＝|adv(t)−adv(t−1)| 的全局平均；(b) 領先易手次數＝adv 跨零符號變化次數（|adv|<0.02 視為零帶不計）；(c) 決勝時點＝最後一次跨零的回合 ÷ 對局總回合。
4. **安全網決定勝負率**：分子＝「致勝一擊來源是安全網傷害」或「敗者總失血中安全網佔比 ≥50%」的對局；分母＝有勝負的對局。同時保留舊觸發率並列輸出。

測試：每指標至少 2 個手工構造軌跡的已知值測試（例：單調碾壓局翻盤率 0、V 型逆轉局翻盤率 1、安全網補刀局判定為網決定）。收工報告附：對 dir3 v1.1 基準（greedy 對 turtle、n=300、seed 照約定）跑出的三指標數值當 demo。

## A2 攻守幾何博弈（不改引擎）——工作目錄 `work/a2_geometry/`

問題：守方會走位時，v0 範圍詞彙 7 形狀各值多少（覆蓋矩陣只量過靜態）。
- 建零和矩陣賽局：行＝（形狀×合法施放原點）的組合；列＝守方在己半場 9 格的站位；payoff＝該形狀從該原點是否覆蓋守方格（1/0）。形狀與覆蓋判定**直接 import engine_base 的幾何函式**，不重寫。
- 用虛擬對局法（fictitious play）解混合均衡，迭代 ≥50,000 次、收斂判準：exploitability < 0.005；純標準庫實作。
- 輸出三層：①各形狀的均衡總權重（原點權重加總）；②守方 9 格均衡分佈；③賽局值（均衡命中率）。
- 變體二「黏性走位」：守方列改為（前一格,可及範圍±1 步）的策略、以兩期重複賽局近似——若實作成本高，允許降級為「守方限定相鄰格轉移的受限混合」並在報告標明近似法。
- 判讀欄：零權重形狀清單（廢牌候選）、權重集中度（最大形狀權重 >0.5＝形狀單一化警訊）、守方分佈熵。

## B2 先導（深度階梯可行性）——工作目錄 `work/b2_pilot/`

問題：更強 AI 的效能可不可行；淺階梯有沒有訊號。
1. 複製 engine_base 到工作目錄，新增 twoply 策略：對自己整回合行動組合取靜態估值前 K=3，對每個再枚舉對手回應的估值前 K=3，取 minimax 期望。估值器用 policies.py 既有 greedy 估值、不新設計權重。
2. 實測單局壁鐘：dir3 與 dir2 各測 greedy/oneply/twoply 三檔（n=10 局、報中位與 p90）。
3. 淺階梯訊號測試：dir3 上 depth0(greedy) vs depth1(oneply) vs depth2(twoply) 循環賽，每配對 n=40（twoply 涉入的配對可降到 n=20，標明）。指標：高深度對低深度勝率是否單調 >0.5。
4. **產出 B2 全量的成本預估與建議**（這是先導的主要交付）：以實測單局秒數推估「3 方向×3 深度×每配對 n=100」總壁鐘。門檻寫死：≤4 小時→建議全量照跑；4–12 小時→縮減設計（只跑 dir2＋dir1、序貫停止）；>12 小時→只做淺階梯方法驗證。報告給明確建議檔位。

## A1 猜測訊號對行動數（不改引擎，dir3 底座）——工作目錄 `work/a1_signal/`

問題：題1 核心張力「行動越少、放棄越是訊號」從沒直測。
- 格：行動數/回合 ∈ {1,2,3} × 同類限制 {開,關}＝6 格（行動數1 時同類限制自然無效，仍跑當對照）。改行動數若 setups/core 有參數就用參數；沒有就在自己的引擎副本最小改動（標明 diff）。
- 每格 n=200 局（greedy 對 turtle 各半＋greedy 鏡像各半）。
- 主指標「**可推算度**」（尺③的反向）：對每個對手回合，抽 200 副與公開資訊一致的替代手牌，用同一 greedy 估值器問「這副手牌會不會產生實際觀察到的行動」；可推算度＝1 − 一致手牌比例（行動把手牌空間砍掉多少）。**沿用 pillars.py 的抽樣機制改造，嚴防上輪教訓：估值基準必須同一把尺、抽樣行動必須＝實際打出的行動。**
- 副指標：尺③、尺①（pillars.py 現成）、對局長度、先手勝率。
- 判讀欄：可推算度對行動數是否單調遞減（＝張力假說成立方向）；同類限制開關的增量。

## A3 蓄力承諾博弈（獨立小模型＋引擎傷害數字）——工作目錄 `work/a3_charge/`

問題：發想_蓄力動作從沒建模；均衡是純策略＝死機制。
- 兩期小賽局，傷害數字全部取自 v1.1 檔位與 chips.py 傷害帶（不自創數值）：蓄力方 {蓄力(下回合傷害×m), 直攻}；反應方在反應窗 r 個行動內 {打斷(對蓄力者出傷), 防禦(掛盾/閃), 無視(推進自己輸出)}。payoff＝兩期 HP 淨差（零和）。
- 格：m ∈ {1.5, 2.0, 3.0} × r ∈ {1, 2}＝6 格。每格解混合均衡（fictitious play 同 A2 標準）。
- 打斷傷害、防禦減免用引擎 `resolve_attack` 實算代表性組合（至少：自攻10、中檔晶片25、高檔晶片40 對 閃4/盾10/場5），取期望、報告列出所用組合。
- 判讀欄：均衡蓄力機率 p；p∈(0.1,0.9)＝活機制（虛實博弈存在）；p 貼 0 或 1＝死機制；找出機制由死轉活的臨界 m。

## A4 排序深度（不改引擎，dir3＋dir2 狀態抽樣）——工作目錄 `work/a4_ordering/`

問題：護盾回充窗是否真造成「出牌順序有學問」，還是純記帳負擔。
- 從 dir3 與 dir2 各 n=150 局（greedy 混 turtle）中抽「本回合有 ≥2 個可執行攻擊行動」的決策狀態各 500 個。
- 對每狀態枚舉該回合行動的全部排序，用引擎結算各排序的總傷害（含護盾回充窗語義）；記 spread＝max−min。
- 對照組：同批狀態在「盾歸零後不回充」變體下重算 spread（在引擎副本開最小開關、附 diff 與測試）。
- 判讀欄：spread>0 的狀態比例、spread≥10（≈一檔傷害帶）的比例、回充窗開關的增量。增量趨零＝回充窗是純記帳、題4 素材。

## A5 先手優勢解剖（需 B6 的 metrics v2）——工作目錄 `work/a5_firstmove/`

問題：三檔補償全失效且方向間反向，病根沒診斷過。
- 引擎用 `work/b6_engine_v2/`（主幹驗過才會派你，直接用）。
- 三方向 v1.1 檔位、**補償一律關閉**（要量裸優勢）：每方向 greedy 鏡像 n=1000＋greedy對turtle n=500。
- 量測：(a) 先手方優勢軌跡 adv(t) 均值曲線（B6 指標1）；(b) 優勢開口回合＝adv 均值首次持續（連續 3 回合）>0.05 的回合；(c) 首傷歸屬率（先手方先開張的比例）；(d) 行動數差累計曲線；(e) 方向二加「首顆資源歸屬率」。
- 判讀欄：逐方向診斷「優勢在第幾回合、經什麼管道形成」＋對症補償建議（例：開口在首回合首傷→傷害上限類；開口在資源先佔→資源側補償；開口緩慢累積→輪替式補償）。此欄標「素材、待主幹綜合」，不下定論。

## A6 僵持解剖＋資源紅利化（改 dir2，最大電池）——工作目錄 `work/a6_stall/`

問題：方向二 66% 平局、83% 觸網的僵持三因沒分解過；資源 0.99 壓倒性要降級成紅利。
- 引擎用 `work/b6_engine_v2/` 複製出去改 dir2（附 diff＋新測試，舊測試照綠）。
- **Part 1 僵持三因分解**：基準 dir2 v1.1 n=500 局（策略混合照卷宗_dir2 的循環賽組合、oneply 除外），對每個零傷害回合分類（判定順序寫死，先 1 後 2 後 3）：
  1. 打不到＝該回合不存在任何能命中對手的合法攻擊行動；
  2. 打不痛＝存在能命中的行動但全部期望淨傷 ≤0（被防禦全吸收）；
  3. 不想打＝存在期望淨傷 >0 的行動但策略選了別的（撿資源/走位/佈陷阱）。
  輸出：三因全局占比＋按對局前/中/後三段的占比；連續零傷段（追逐死結）長度分佈。
- **Part 2 資源四變體**（每變體 n=500：懂資源 對 不懂資源＋turtle 對 greedy 兩組）：
  V1 效果減半；V2 雙方自動獲取（每 2 回合雙方直接+1 顆資源效果、場上不再生成、走位誘因歸零＝隔離「誘因」與「資源量」）；V3 只加速不解鎖（資源只給+1 行動點該回合用、不再解鎖任何能力/加傷）；V4 撿取有代價（撿取者至其下回合結束防禦值−25%，模擬「暴露位置」的風險代理，報告標明是代理量）。
  指標：aware對blind 勝率（目標從 0.99 往 0.6–0.75 帶走）、turtle 平局率、對局長度中位、移動面被選率、安全網決定勝負率（B6 新指標）。
- 判讀欄：哪個變體「保留反龜承重（aware>blind 顯著）但擺脫必需品性（<0.85）」；三因分解對題7 的含意。標「素材、待主幹綜合」。

---

## 對抗校核（輪1 收尾、主幹另派 opus）

範圍：上列全部報告＋程式。攻擊清單（至少）：估值器基準不一致（尺③教訓）、抽樣行動≠實際行動、優勢代理量混淆（HP差≠勝率）、均衡解收斂性（exploitability 實測值）、A6 三因分類的判定順序偏差、翻盤率分母處理、同 seed 重跑抽驗 2 格。裁決：逐實驗「通過／需修正（具體點）」。
