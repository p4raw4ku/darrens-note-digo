--- engine_base/search_v1.py	2026-07-05 23:41:16
+++ work/a1_signal/search_v1.py	2026-07-06 01:03:15
@@ -363,8 +363,13 @@
 
 
 def make_v1_config(config, seed, oneply_samples=50, expect_samples=50,
-                   first_turn_comp_override=None):
-    """把 config dict 轉成 GameConfig。先手補償含新候選『後手多抽2』。"""
+                   first_turn_comp_override=None,
+                   actions_per_turn=2, same_category_limit=True):
+    """把 config dict 轉成 GameConfig。先手補償含新候選『後手多抽2』。
+
+    actions_per_turn / same_category_limit：A1 實驗新增旋鈕（core.GameConfig
+    新欄位的傳遞入口，見 core.py diff）；預設值＝原行為，不影響既有呼叫方。
+    """
     comp = first_turn_comp_override or config['先手補償']
     starting_hand = 4
     draw_per_turn = 1
@@ -381,7 +386,8 @@
         safety_net_turn=config['安全網起點'], max_turns=60, seed=seed,
         oneply_samples=oneply_samples, expect_samples=expect_samples,
         hand_limit=6, draw_per_turn=draw_per_turn, starting_hand=starting_hand,
-        extra=extra)
+        extra=extra, actions_per_turn=actions_per_turn,
+        same_category_limit=same_category_limit)
     return cfg
 
 
